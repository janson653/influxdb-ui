# Suggested Commands

## Development Commands
- `pnpm install` - Install dependencies
- `pnpm dev` - Start development server (port 1420)
- `pnpm tauri dev` - Start Tauri development mode
- `pnpm build` - Build the application
- `pnpm tauri build` - Build Tauri desktop application
- `pnpm preview` - Preview production build
- `tsc --noEmit` - Type checking

## Quality Commands
- No ESLint/Prettier configuration found - manual code style checking required

## Build and Test
- No test scripts configured in package.json
- Build process: TypeScript compilation + Vite build + Tauri packaging